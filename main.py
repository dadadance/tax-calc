"""Main entry point for tax-calc."""
# Run with: streamlit run app.py
# Or: uv run streamlit run app.py

if __name__ == "__main__":
    print("To run the app, use: streamlit run app.py")
    print("Or: uv run streamlit run app.py")
